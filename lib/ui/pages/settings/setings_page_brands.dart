import 'package:flutter/material.dart';

class SettingsPageBrands extends StatelessWidget {
  const SettingsPageBrands({super.key});

  @override
  Widget build(BuildContext context) {
    return Builder(
      builder: (context) {
        return Column(
          children: [],
        );
      },
    );
  }
}
